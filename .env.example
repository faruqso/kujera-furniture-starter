# Production URL for canonical links and social preview images
# SITE_URL=https://your-domain.com

# ── Decap CMS (production) ──
# Full guide: docs/decap-setup.md
#
# Local editing uses `npm run cms` — no env vars required.
# For saves on a deployed /admin, configure GitHub backend in public/admin/config.yml
# and set up Netlify Identity or a custom OAuth proxy.
