---
interface Props {
  variant?: 'brand' | 'sale' | 'success' | 'muted';
  class?: string;
}

const { variant = 'brand', class: className = '' } = Astro.props;
---

<span class={`badge badge--${variant} ${className}`.trim()}>
  <slot />
</span>
