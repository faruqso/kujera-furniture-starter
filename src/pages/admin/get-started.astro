---
import {
  getTemplateForkUrl,
  getTemplateRepoUrl,
  isDecapEditEnabled,
} from '../../lib/decap-public';

export const prerender = false;

const reason = Astro.url.searchParams.get('reason');
const editEnabled = isDecapEditEnabled();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Set up Decap CMS | Kujera Furniture Starter</title>
    <style>
      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        font-family: system-ui, sans-serif;
        background: #f8fafc;
        color: #0f172a;
        line-height: 1.5;
      }

      main {
        max-width: 40rem;
        margin: 0 auto;
        padding: 3rem 1.5rem;
      }

      h1 {
        font-size: 1.75rem;
        margin: 0 0 0.5rem;
      }

      .lead {
        color: #475569;
        margin: 0 0 2rem;
      }

      .notice {
        padding: 1rem 1.25rem;
        margin-bottom: 2rem;
        border-radius: 8px;
        background: #eff6ff;
        border: 1px solid #bfdbfe;
        color: #1e3a8a;
      }

      ol {
        padding-left: 1.25rem;
        margin: 0 0 2rem;
      }

      li + li {
        margin-top: 0.75rem;
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
      }

      a.btn {
        display: inline-block;
        padding: 0.65rem 1.1rem;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.9375rem;
      }

      a.btn-primary {
        background: #0f172a;
        color: #fff;
      }

      a.btn-secondary {
        background: #fff;
        color: #0f172a;
        border: 1px solid #cbd5e1;
      }

      .back {
        display: inline-block;
        margin-top: 2rem;
        color: #64748b;
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Set up your own CMS</h1>
      <p class="lead">
        {
          reason === 'save'
            ? 'Saving changes requires your own copy of this starter and a GitHub backend connected to your repo.'
            : reason === 'edit'
              ? 'Editing entries requires your own copy of this starter and a GitHub backend connected to your repo.'
              : 'This starter lets anyone browse the CMS demo. To edit and publish content, use your own fork.'
        }
      </p>

      {
        editEnabled ? (
          <div class="notice">
            Editing is enabled on this deployment. Configure GitHub OAuth in your host env vars to sign in
            and save.
          </div>
        ) : (
          <div class="notice">
            This deployment is in <strong>browse-only</strong> mode. Follow the steps below to edit on your
            own site.
          </div>
        )
      }

      <ol>
        <li>
          <a href={getTemplateForkUrl()}>Fork the template</a> to your GitHub account (
          <code>{getTemplateRepoUrl()}</code>)
        </li>
        <li>
          Set <code>PUBLIC_DECAP_REPO</code> on your host to your <code>username/repo</code>
        </li>
        <li>
          Follow the{' '}
          <a href="https://github.com/faruqso/kujera-furniture-starter/blob/main/docs/decap-setup.md">
            Decap CMS setup guide
          </a>{' '}
          to configure GitHub OAuth on your host
        </li>
        <li>
          Set <code>PUBLIC_DECAP_EDIT_ENABLED=true</code> on your deployment when you are ready to allow saves
        </li>
      </ol>

      <div class="actions">
        <a class="btn btn-primary" href={getTemplateForkUrl()}>Fork on GitHub</a>
        <a
          class="btn btn-secondary"
          href="https://github.com/faruqso/kujera-furniture-starter/blob/main/docs/decap-setup.md"
        >
          Read setup guide
        </a>
      </div>

      <a class="back" href="/admin/">← Back to CMS dashboard</a>
    </main>
  </body>
</html>
